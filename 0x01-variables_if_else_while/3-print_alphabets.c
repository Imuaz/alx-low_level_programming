#include <stdio.h>


/**
 * main-prints out mix alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter;

	for (letter = 'a'&& letter = 'A'; letter <= 'z'&& letter = 'Z'; letter++)
		putchar(letter);
	putchar('\n');

	return (0);
}
