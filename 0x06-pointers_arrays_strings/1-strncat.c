#include "main.h"

/**
 * *_strncat - concatenates two strings just like strcat but uses n-bytes from
 * src and src does not need null terminating character '\0'
 * @dest: the destination string
 * @scr: the source string
 * Return:a pointer to the resulting dest string
 */
char *_strncat(char *dest, char *src, int n)
{

